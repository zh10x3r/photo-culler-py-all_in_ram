# Photo Culler：功能实现说明

## 1. 软件目标

Photo Culler 是一个面向 Windows 摄影工作流的快速选片工具。它不负责调色、裁剪或修改照片，只负责：

1. 打开一个照片文件夹。
2. 快速浏览照片并进行保留/取消保留标记。
3. 识别同一次拍摄产生的 RAW+JPG 文件，避免重复显示。
4. 按选择结果把原始文件复制到另一个文件夹。

软件的核心原则是“来源文件只读”：浏览和选片不会修改、移动或重命名原始照片。

## 2. 使用的技术

- **Python 3.13**：主要开发语言。
- **PySide6 + Qt**：正式版 Windows 主窗口、按钮、复选框、分隔栏、缩略图列表和完整事件循环。
- **VisPy/OpenGL**：正式版中央 GPU 预览；纹理上传一次后，缩放、平移和插值由 GPU 完成。
- **Tkinter/ttk**：保留在源码中的旧 `PhotoCuller` 类，用于兼容已有纯逻辑回归测试和旧版诊断代码，不再作为正式启动入口。
- **Pillow**：读取 JPG、JPEG、PNG、TIFF，处理 EXIF 方向并生成后台缩略图；不再为每个 GPU 滚轮事件重新生成主预览帧。
- **rawpy/LibRaw**：读取 DNG 的内嵌预览图；没有内嵌预览时，解码较小的 RAW 预览。
- **NumPy + PyOpenGL**：把已解码的 RGB 图像整理为连续数组，并读取实际 OpenGL 上下文的显卡信息。
- **PyInstaller**：将程序及依赖打包成单个 `.exe`。
- **Tcl/Tk 运行库**：随最终程序一起打包，避免依赖用户电脑上可能不完整的 Python Tk 安装。

## 3. 界面结构

界面分成五个区域：

1. **顶部信息栏**：显示当前打开的文件夹名称，不放置操作按钮。
2. **右侧操作栏**：纵向放置打开文件夹、保留/取消、模式、重置、导出、适合屏幕、100% 和“只看保留”等全部按钮或选项。整个操作栏由 Qt `QScrollArea` 承载，控件超出可用高度时只滚动菜单内容，不会挤压中央预览；左侧分隔线可以拖动调整宽度，宽度会保存到本机设置。
3. **中央大图区域**：显示当前照片，支持以鼠标位置为中心缩放以及放大后的拖动平移。
4. **状态栏**：显示当前序号、保留状态、RAW/JPG 模式和 JPG 预载进度。
5. **底部缩略图栏**：显示当前文件夹中的照片组，黄色星号表示已保留。
6. **GPU 预览设置面板**：右侧默认只显示一个展开按钮，点击后在可滚动菜单中展开中央 GPU 预览的插值方式、平滑滚轮和最大倍率；展开时程序会自动把设置面板滚动到可见区域，设置保存在本机设置文件中。

底部缩略图不是一次性把所有大图加载到界面中，而是由 Qt `QListView` 只绘制当前可见范围，并在后台生成小尺寸缩略图，从而避免照片数量较多时一次创建几千个界面图片对象。

当前缩略图渲染还做了四层优化：

1. **视图虚拟化**：`QListView`/delegate 只为可见 tile 调用绘制，不创建与照片总数等量的画布项目。
2. **可见范围加小预取**：滚动时请求当前视口前后少量缩略图，避免照片数量很大时同时启动全部解码任务。
3. **后台生成小图**：从 JPG 内存缓存或其他格式文件读取图像、生成缩略尺寸在线程池完成；Qt 主线程只负责将完成的 PIL 图像转换为 `QPixmap`。
4. **有界 LRU 缓存**：缩略图缓存最多保留 `THUMB_CACHE_LIMIT` 项，缓存键包含路径、修改时间、文件大小和尺寸，文件变化后不会误用旧缩略图。

## 4. 文件读取与格式支持

打开文件夹时，程序会递归扫描用户选择的根文件夹及其普通子文件夹。扫描只读取目录项和文件元数据，不会在扫描阶段解码每一张图片。支持的扩展名为：

- `.jpg`
- `.jpeg`
- `.png`
- `.tif`
- `.tiff`
- `.dng`

递归扫描由 `scan_photo_tree()` 完成，采用 `os.scandir()` 和目录栈，而不是 Python 递归调用。`DirEntry` 可以直接提供文件/目录类型信息，减少额外的元数据查询。扫描放在单独的后台线程中，主线程通过队列接收“已发现数量、已访问目录数、异常数量”等进度事件。

扫描完成后，程序把路径按相对根文件夹的路径排序，再一次性替换当前照片组列表。这样缩略图索引、键盘导航和 JPG 预载都基于同一份完整快照，不会在扫描中途因为列表重新排序而错位。某个子文件夹或文件读取失败时会记录异常并继续扫描其他分支。

默认不跟随符号链接目录和 Windows junction/reparse 目录。这样可以避免递归到所选根目录之外，也可以避免链接环造成无限扫描；普通隐藏子文件夹仍然会被访问。

普通图片读取流程如下：

1. 用 Pillow 打开文件。
2. 使用 `ImageOps.exif_transpose()` 处理相机 EXIF 方向。
3. 转为 RGB。
4. 复制到内存，使文件句柄可以立即关闭。

DNG 的读取流程如下：

1. 用 rawpy 打开 DNG。
2. 优先调用 `extract_thumb()`，读取相机写入的内嵌预览图。
3. 如果没有可用内嵌预览，再调用 LibRaw 生成 `half_size=True` 的屏幕预览。
4. 导出时不重新保存 DNG，而是直接复制用户的原始 DNG 文件。

## 5. RAW+JPG 自动绑定逻辑

### 5.1 为什么要分组

相机使用 RAW+JPG 拍摄时，通常会生成例如：

```text
DSC_0001.DNG
DSC_0001.JPG
```

如果直接按文件显示，会让同一次拍摄出现两次。因此程序先把文件转换为“照片组”，选片操作针对的是组，而不是单个路径。

### 5.2 分组规则

程序会按“不区分大小写的父文件夹路径 + 文件名主干”进行归类：

```text
DSC_0001.DNG  -> DSC_0001
DSC_0001.JPG  -> DSC_0001
```

只有同一文件夹中同名的 DNG 与 JPG/JPEG 同时存在时，才会合并为 RAW+JPG 绑定组。不同子文件夹中的同名文件不会互相绑定；PNG、TIFF 等文件也不会因为同名就被误绑定进 RAW+JPG 组。

每个绑定组有：

- `primary`：界面预览用的代表文件，优先选择 JPG/JPEG。
- `members`：该组实际包含的所有原始文件。
- `key`：用于保存选片状态的稳定标识。

因此界面只显示一张缩略图，但导出时仍然能找到该组包含的 RAW 和 JPG 原文件。

## 6. 两个完全独立的选片状态

RAW+JPG 组现在有两个相互独立的状态变量：

### 6.1 是否保留

用 `kept` 集合记录哪些照片组被选中：

```text
kept = {组 A, 组 C, 组 F}
```

按 `Space` 只会改变这个状态：

- 未保留 → 保留
- 已保留 → 取消保留

按 `Space` 不会改变 RAW/JPG 模式。

### 6.2 保存什么

用 `pair_modes` 字典记录 RAW+JPG 组的导出模式：

```text
pair_modes = {
    组 A: "both",
    组 C: "raw",
    组 F: "jpg",
}
```

模式含义：

- `both`：导出 RAW+JPG，默认模式。
- `raw`：只导出 DNG。
- `jpg`：只导出 JPG/JPEG。

按 `F` 或点击模式按钮，只改变 `pair_modes`，不会自动把照片变成保留状态。循环顺序为“RAW+JPG → 仅 JPG → 仅 RAW → RAW+JPG”。即使一组照片当前未保留，也可以先设置它将来要导出的类型。

取消保留时，原来的 RAW/JPG 模式仍然保留；再次按 `Space` 选中时，会恢复到之前的模式。

## 7. 导出逻辑

导出时程序先筛选 `kept` 中的照片组，然后根据组模式决定要复制的成员：

```text
是否保留 = 否       -> 不复制
是否保留 = 是
    模式 both       -> 复制 DNG + JPG
    模式 raw        -> 只复制 DNG
    模式 jpg        -> 只复制 JPG/JPEG
```

复制使用 `shutil.copy2()`，尽量保留原始文件的时间等属性。目标文件夹中如果已经存在同名文件，程序会自动生成：

```text
照片.jpg
照片 (1).jpg
照片 (2).jpg
```

这样不会覆盖已有导出文件。

## 8. JPG 内存预载

为了减少连续浏览时的磁盘等待，打开文件夹后程序会启动后台线程，预先读取所有 JPG/JPEG：

1. 后台扫描线程收集根文件夹及所有子文件夹中的照片路径。
2. 扫描完成后，后台线程逐张把 JPG 解码成 RGB 图像。
3. 图像保存在 `jpeg_cache` 字典中。
4. 状态栏显示“正在预载 JPG：n / 总数”。
5. 预载完成后显示“JPG 已预载”。

RAW+JPG 组只预载 JPG，不会把 RAW 解码进内存。原始 RAW 仍然只在需要显示 DNG 时按需读取，导出时直接复制原文件。

这种设计用内存换取浏览速度。照片数量特别多或单张 JPG 分辨率非常高时，内存占用会明显增加；切换到另一个文件夹时，上一文件夹的 JPG 缓存会被释放，旧扫描或旧预载任务的结果会通过代次编号丢弃。

### 8.2 底部缩略图的内存与绘制边界

这次优化没有改变“所有 JPG/JPEG 全量预载到 `jpeg_cache`”的策略：大图仍然保留在内存中，以便左右切换时快速显示。变化只发生在底部胶片条：

- `jpeg_cache` 保存的是 JPG 的完整 RGB 图像；
- `thumbnail_cache` 只保存最近使用的一小批 Qt `QPixmap` 缩略图；
- `QListView` 的 delegate 只绘制当前可见范围，后台线程生成的是临时的小尺寸 PIL 图像，交给 Qt 主线程转换后即可释放临时对象。

因此内存主要仍由用户主动选择的“全量 JPG 预载”决定，而不是由照片总数对应的 Canvas 绘制对象决定。

## 9. GPU 缩放与平移

正式版中央预览由 `QtGpuPreviewWidget` 承载 VisPy `SceneCanvas`，相机为 `QtSmoothPanZoomCamera`。照片只在切换当前组时转换为连续 RGB NumPy 数组并上传一次，之后的滚轮缩放、左键拖动和插值查找都发生在 OpenGL 渲染路径中；`jpeg_cache` 中的完整 JPG 不会被修改。

### 9.1 滚轮缩放

- 每五个标准滚轮刻度约改变一倍倍率，单次设备事件限制在四个刻度，避免异常滚轮值造成跳变。
- 连续滚轮输入合并为目标纹理宽度，`QtSmoothPanZoomCamera` 使用 120 Hz VisPy 定时器按指数响应逐帧追赶目标；因此快速滚轮会表现为连续的捏合式动画，而不是直接瞬移。
- 缩放以鼠标下方的源坐标为锚点，缩放过程中该位置保持在光标附近；按住左键拖动会停止当前动画并交给 VisPy 相机直接平移。
- “平滑滚轮缩放”关闭时仍使用同一 GPU 相机，但立即应用目标倍率。最大倍率由右侧 4/8/16/32 倍设置限制。

### 9.2 插值与纹理边界

右侧设置可以切换 `cubic`、`catrom`、`linear`、`nearest`。新照片可能与上一张尺寸不同，上传后会显式设置 `ImageVisual._need_interpolation_update`，强制重新建立 lookup shape，避免首帧只有颜色或严重模糊。初始化时不调用预显示 `canvas.render()`；窗口显示并取得真实 GL 上下文后才查询 GPU、创建纹理并绘制。

如果照片边长超过显卡报告的 `GL_MAX_TEXTURE_SIZE`，程序只为预览生成一份不超过上限的临时缩小纹理，导出仍复制原始文件；常见相机 JPG 通常不会触发该分支。

## 10. Qt 主窗口迁移

正式启动入口是 `QtPhotoCuller(QMainWindow)`，主线程直接运行 Qt 事件循环和 VisPy `pyside6` backend。右侧控制栏、可拖动分隔线、GPU 设置、递归扫描、全量 JPG 预载、RAW+JPG 选片状态和导出都在同一个 Qt 窗口中完成，不再通过 Tk→Qt 事件泵驱动独立调试窗口。

1. 扫描线程只负责发现路径；完成后在 Qt 主线程安装完整 `PhotoGroup` 快照。
2. JPG 预载线程仍把根目录及所有子目录的 JPG/JPEG 解码到 `jpeg_cache`；DNG 仍按需读取内嵌预览或半尺寸 RAW。
3. `QListView` 使用自定义 delegate，视图只绘制可见缩略图；后台线程生成 PIL 小图，主线程转换为 `QPixmap`，并以有界 LRU 缓存最近项目。
4. `kept` 与 `pair_modes` 仍是两个独立状态；`Space` 只改变保留状态，`F` 只循环 `both → jpg → raw`，导出只复制已保留组当前模式指定的成员。
5. Qt `QAction` 使用应用级快捷键，保留 `[ ]`、`Space`、`F`、`O`、`E`、`Z`、`1`、`+/-`、`Ctrl+Shift+X/M`；缩放和平移由中央 VisPy canvas 原生接收鼠标事件。

### 10.1 OpenGL 生命周期保护

`QOpenGLWidget` 必须在窗口显示并拥有真实上下文后才能查询 GPU 或进行首帧绘制。集成窗口构造时不调用 `canvas.render()`；`show()` 后通过 `initialize_after_show()` 查询 vendor、renderer、OpenGL 版本和单纹理上限，再上传首张图片。正式版提供 `--gpu-self-test`，用隐藏窗口验证：

- 实际 PySide6 backend 能创建 OpenGL 上下文；
- 首帧渲染尺寸为 `960 × 640` 且像素非零；
- 首次上传 `1800 × 1200` 图片后 cubic 查找形状同步为 `[1800, 1200]`；
- 指针锚定缩放能产生约 1.15 倍的渐进变化，动画结束后定时器停止；
- 不存在预显示 `canvas.render()` 的 FBO 回归路径。

源码仍保留旧 Tk 类和独立 GPU 调试构造函数，以便旧版纯逻辑测试和诊断命令继续工作；正式 EXE 的正常入口使用 Qt 主窗口。没有 PySide6/VisPy 时，Qt 入口会显示明确的依赖错误，而不会悄悄退回一套渲染效果不同的主界面。

## 11. 高 DPI 界面处理

最初版本在高分辨率显示器上可能被 Windows 位图缩放，造成文字和按钮发糊。现在启动时会尝试启用：

```text
PER_MONITOR_AWARE_V2
```

Qt 会按 Windows 的设备像素比绘制主窗口和 VisPy canvas；缩略图使用逻辑尺寸交给 Qt 进行高 DPI 换算。源码中的旧 Tk 类仍保留原有 DPI 调整逻辑。

最终 `.exe` 还自带一套 Tcl/Tk 运行库。这样即使系统 Python 的 Tk 目录结构不完整，程序也能使用自己的图形运行环境。

## 12. 选片记录保存

选片状态不会写入原照片文件夹，而是保存在当前 Windows 用户的本地应用数据目录：

```text
%LOCALAPPDATA%\PhotoCuller\selections\<文件夹哈希>.json
```

文件中主要保存：

- 当前照片文件夹路径。
- `kept`：哪些组被保留。
- `pair_modes`：每个 RAW+JPG 组导出什么内容。
- 最后更新时间。

旧版本只有 `kept` 数据时，升级后 RAW+JPG 组会按默认的 `both` 模式处理。

## 13. 快捷键与按钮

| 功能 | 快捷键 |
|---|---|
| 上一组照片 | `[` |
| 下一组照片 | `]` |
| 保留/取消保留 | `Space` |
| 切换当前 RAW+JPG 模式 | `F` |
| 打开照片文件夹 | `O` |
| 导出保留照片 | `E` |
| 适合屏幕 / 100% | `Z` |
| 直接显示 100% | `1` |
| 逐级放大 | `+` / 小键盘 `+` |
| 逐级缩小 | `-` / 小键盘 `-` |
| 鼠标缩放 | 大图区域滚轮 |
| 放大后平移 | 大图区域按住左键拖动 |
| 全部不保留 | `Ctrl+Shift+X` |
| 重置所有 RAW/JPG 模式 | `Ctrl+Shift+M` |

“全部不保留”和“重置所有模式”都会弹出确认框：

- **全部不保留**：清空所有 `kept`，但保留 `pair_modes`。
- **重置所有模式**：把每个 RAW+JPG 组的 `pair_modes` 改为 `both`，但不改变 `kept`。

## 14. 构建与验证

开发目录中使用独立虚拟环境安装 `requirements.txt` 和 PyInstaller。最终打包时额外加入：

- `tkinter` 与 `_tkinter`。
- Tcl/Tk 脚本目录。
- `tcl86t.dll` 与 `tk86t.dll`。
- rawpy 及其 LibRaw 依赖。
- VisPy 的 shader/data 文件、PySide6 的 Qt OpenGL backend、NumPy 和 PyOpenGL。

推荐使用仓库中的 `Photo Culler.spec`，并通过 `PHOTO_CULLER_TK_RUNTIME` 指向包含 `tcl` 和 `bin` 的 Tcl/Tk 运行库目录。spec 会过滤可能被构建环境误收集的 Poppler `icuuc.dll` / `icudt78.dll`，避免 QtCore 在 Windows 上报 WinError 127。

目前做过的自动检查包括：

1. Python 源码编译检查。
2. RAW+JPG 分组测试。
3. 仅 RAW、仅 JPG、RAW+JPG 导出成员选择测试。
4. 保留状态与模式独立性的测试。
5. Bicubic 过滤器、滚轮目标倍率、平滑过渡、鼠标锚点和可见区域裁剪测试。
6. `app.py --gpu-self-test` 的 PySide6/OpenGL、首帧插值尺寸和动画生命周期自检；`app.py --qt-main-self-test` 直接启动正式 Qt 主窗口路径做同样检查。
7. GPU 事件泵在动画/空闲状态下使用不同调度间隔的回归测试。
8. 递归扫描、同目录绑定和链接目录跳过测试。
9. 打包后的 `.exe --self-test` 无窗口运行库自检，退出码为 0。

按照使用者要求，没有通过 Computer Use 自动操作用户桌面做视觉测试；因此实际照片文件夹中的显示效果仍应由用户在自己的显示器上进行最后确认。

## 15. 当前边界

- 当前 RAW 专门支持 DNG；其他相机 RAW 格式尚未加入。
- 当前会递归扫描所选文件夹及其普通子文件夹；符号链接和 Windows junction/reparse 目录不会跟随。
- 软件是选片工具，不包含调色、裁剪、删除和照片修改功能。
- RAW+JPG 组的预览使用 JPG，不提供手动切换查看 RAW 内嵌预览的功能。
- JPG 全量预载会使用较多内存，这是用内存换取浏览速度的设计选择。
