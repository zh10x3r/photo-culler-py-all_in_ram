# Photo Culler：功能实现说明

## 1. 软件目标

Photo Culler 是一个面向 Windows 摄影工作流的快速选片工具。它不负责调色、裁剪或修改照片，只负责：

1. 打开一个照片文件夹。
2. 快速浏览照片并进行保留/取消保留标记。
3. 识别同一次拍摄产生的 RAW+JPG 文件，避免重复显示。
4. 按选择结果把原始文件复制到另一个文件夹。

软件的核心原则是“来源文件只读”：浏览和选片不会修改、移动或重命名原始照片。

## 2. 使用的技术

- **Python 3.13**：主要开发语言。
- **Tkinter/ttk**：Windows 桌面界面、按钮、复选框和画布。
- **Pillow**：读取 JPG、JPEG、PNG、TIFF，进行缩放、裁剪和 EXIF 方向处理。
- **rawpy/LibRaw**：读取 DNG 的内嵌预览图；没有内嵌预览时，解码较小的 RAW 预览。
- **PyInstaller**：将程序及依赖打包成单个 `.exe`。
- **Tcl/Tk 运行库**：随最终程序一起打包，避免依赖用户电脑上可能不完整的 Python Tk 安装。

## 3. 界面结构

界面分成四个区域：

1. **顶部工具栏**：打开文件夹、导出、缩放控制、RAW/JPG 模式、全部不保留、重置模式。
2. **中央大图区域**：显示当前照片，负责缩放和平移。
3. **状态栏**：显示当前序号、保留状态、RAW/JPG 模式和 JPG 预载进度。
4. **底部缩略图栏**：显示当前文件夹中的照片组，黄色星号表示已保留。

底部缩略图不是一次性把所有大图加载到界面中，而是只为当前可见范围生成 Tk 缩略图，从而避免照片数量较多时一次创建几千个界面图片对象。

## 4. 文件读取与格式支持

当前扫描的是用户选择文件夹的第一层文件，不递归进入子文件夹。支持的扩展名为：

- `.jpg`
- `.jpeg`
- `.png`
- `.tif`
- `.tiff`
- `.dng`

普通图片读取流程如下：

1. 用 Pillow 打开文件。
2. 使用 `ImageOps.exif_transpose()` 处理相机 EXIF 方向。
3. 转为 RGB。
4. 复制到内存，使文件句柄可以立即关闭。

DNG 的读取流程如下：

1. 用 rawpy 打开 DNG。
2. 优先调用 `extract_thumb()`，读取相机写入的内嵌预览图。
3. 如果没有可用内嵌预览，再调用 LibRaw 生成 `half_size=True` 的屏幕预览。
4. 导出时不重新保存 DNG，而是直接复制用户的原始 DNG 文件。

## 5. RAW+JPG 自动绑定逻辑

### 5.1 为什么要分组

相机使用 RAW+JPG 拍摄时，通常会生成例如：

```text
DSC_0001.DNG
DSC_0001.JPG
```

如果直接按文件显示，会让同一次拍摄出现两次。因此程序先把文件转换为“照片组”，选片操作针对的是组，而不是单个路径。

### 5.2 分组规则

程序会按不区分大小写的文件名主干进行归类：

```text
DSC_0001.DNG  -> DSC_0001
DSC_0001.JPG  -> DSC_0001
```

只有同名 DNG 与 JPG/JPEG 同时存在时，才会合并为 RAW+JPG 绑定组。PNG、TIFF 等文件不会因为同名就被误绑定进 RAW+JPG 组。

每个绑定组有：

- `primary`：界面预览用的代表文件，优先选择 JPG/JPEG。
- `members`：该组实际包含的所有原始文件。
- `key`：用于保存选片状态的稳定标识。

因此界面只显示一张缩略图，但导出时仍然能找到该组包含的 RAW 和 JPG 原文件。

## 6. 两个完全独立的选片状态

RAW+JPG 组现在有两个相互独立的状态变量：

### 6.1 是否保留

用 `kept` 集合记录哪些照片组被选中：

```text
kept = {组 A, 组 C, 组 F}
```

按 `Space` 只会改变这个状态：

- 未保留 → 保留
- 已保留 → 取消保留

按 `Space` 不会改变 RAW/JPG 模式。

### 6.2 保存什么

用 `pair_modes` 字典记录 RAW+JPG 组的导出模式：

```text
pair_modes = {
    组 A: "both",
    组 C: "raw",
    组 F: "jpg",
}
```

模式含义：

- `both`：导出 RAW+JPG，默认模式。
- `raw`：只导出 DNG。
- `jpg`：只导出 JPG/JPEG。

按 `F` 或点击模式按钮，只改变 `pair_modes`，不会自动把照片变成保留状态。循环顺序为“RAW+JPG → 仅 JPG → 仅 RAW → RAW+JPG”。即使一组照片当前未保留，也可以先设置它将来要导出的类型。

取消保留时，原来的 RAW/JPG 模式仍然保留；再次按 `Space` 选中时，会恢复到之前的模式。

## 7. 导出逻辑

导出时程序先筛选 `kept` 中的照片组，然后根据组模式决定要复制的成员：

```text
是否保留 = 否       -> 不复制
是否保留 = 是
    模式 both       -> 复制 DNG + JPG
    模式 raw        -> 只复制 DNG
    模式 jpg        -> 只复制 JPG/JPEG
```

复制使用 `shutil.copy2()`，尽量保留原始文件的时间等属性。目标文件夹中如果已经存在同名文件，程序会自动生成：

```text
照片.jpg
照片 (1).jpg
照片 (2).jpg
```

这样不会覆盖已有导出文件。

## 8. JPG 内存预载

为了减少连续浏览时的磁盘等待，打开文件夹后程序会启动后台线程，预先读取所有 JPG/JPEG：

1. 主线程扫描照片并显示界面。
2. 后台线程逐张把 JPG 解码成 RGB 图像。
3. 图像保存在 `jpeg_cache` 字典中。
4. 状态栏显示“正在预载 JPG：n / 总数”。
5. 预载完成后显示“JPG 已预载”。

RAW+JPG 组只预载 JPG，不会把 RAW 解码进内存。原始 RAW 仍然只在需要显示 DNG 时按需读取，导出时直接复制原文件。

这种设计用内存换取浏览速度。照片数量特别多或单张 JPG 分辨率非常高时，内存占用会明显增加；切换到另一个文件夹时，上一文件夹的 JPG 缓存会被释放。

## 9. 放大、缩小与平移

中央预览区使用 Tk Canvas，而不是简单的固定大小标签。程序维护以下查看状态：

- `fit_scale`：当前照片适合窗口时的倍率。
- `zoom_scale`：当前实际显示倍率，最高约为 400%。
- `pan_x`、`pan_y`：放大后的平移偏移量。
- 当前源图像：用于反复缩放时避免重新读文件。

### 9.1 为什么只渲染可见区域

如果一张 6000×4000 的照片直接放大到 400%，会产生非常大的临时位图。程序不会把整张放大后的照片全部生成出来，而是：

1. 根据当前倍率和拖动位置计算窗口能看到的源图像区域。
2. 在可见区域外额外保留拖动缓冲范围；该范围按屏幕大小计算，但最多约 560 个实际像素，避免 4K 屏幕生成过大的临时位图。
3. 只从源图像裁出“可见区域 + 缓冲范围”。
4. 将裁剪区域缩放后交给 Canvas 显示。

因此 100% 或 400% 检查时，内存和渲染压力都主要取决于当前可见区域及其小范围缓冲，而不是整张放大后的照片。

### 9.2 交互渲染优化

滚轮和鼠标拖动会产生很多连续事件。如果每个事件都使用最高质量算法重绘，操作会卡顿。现在的策略是：

- 拖动时优先移动 Canvas 中已显示的缓冲画面；通常不需要重新裁剪、缩放或新建 Canvas 图像对象。
- 缓冲画面快接近边缘时，才请求新的快速交互帧。
- 交互帧从当前照片的按需多级缩小版本中读取，并使用较快的双线性缩放。
- 停止操作约 150 毫秒后，在后台从原始像素生成 Lanczos 高质量帧，再回到 Tk 主线程替换显示内容。
- 连续事件会合并；带有旧视图位置的后台结果会按渲染编号丢弃，不能覆盖最新画面。

这样拖动和平滑缩放时优先保证响应，停下来后仍能得到清晰预览。“适合屏幕”模式最终使用原始像素的 Lanczos 结果，不会因交互用的低分辨率帧而永久降低显示质量。

## 10. 高 DPI 界面处理

最初版本在高分辨率显示器上可能被 Windows 位图缩放，造成文字和按钮发糊。现在启动时会尝试启用：

```text
PER_MONITOR_AWARE_V2
```

同时根据显示器 DPI 调整窗口尺寸、缩略图尺寸和 Canvas 坐标，使 Tk 控件按实际像素绘制。

最终 `.exe` 还自带一套 Tcl/Tk 运行库。这样即使系统 Python 的 Tk 目录结构不完整，程序也能使用自己的图形运行环境。

## 11. 选片记录保存

选片状态不会写入原照片文件夹，而是保存在当前 Windows 用户的本地应用数据目录：

```text
%LOCALAPPDATA%\PhotoCuller\selections\<文件夹哈希>.json
```

文件中主要保存：

- 当前照片文件夹路径。
- `kept`：哪些组被保留。
- `pair_modes`：每个 RAW+JPG 组导出什么内容。
- 最后更新时间。

旧版本只有 `kept` 数据时，升级后 RAW+JPG 组会按默认的 `both` 模式处理。

## 12. 快捷键与按钮

| 功能 | 快捷键 |
|---|---|
| 上一组照片 | `[` |
| 下一组照片 | `]` |
| 保留/取消保留 | `Space` |
| 切换当前 RAW+JPG 模式 | `F` |
| 打开照片文件夹 | `O` |
| 导出保留照片 | `E` |
| 适合屏幕 / 100% | `Z` |
| 直接 100% | `1` |
| 放大 | `+` / 小键盘 `+` |
| 缩小 | `-` / 小键盘 `-` |
| 全部不保留 | `Ctrl+Shift+X` |
| 重置所有 RAW/JPG 模式 | `Ctrl+Shift+M` |
| 鼠标缩放 | 大图区域滚轮 |
| 放大后平移 | 大图区域按住左键拖动 |

“全部不保留”和“重置所有模式”都会弹出确认框：

- **全部不保留**：清空所有 `kept`，但保留 `pair_modes`。
- **重置所有模式**：把每个 RAW+JPG 组的 `pair_modes` 改为 `both`，但不改变 `kept`。

## 13. 构建与验证

开发目录中使用独立虚拟环境安装 Pillow、rawpy 和 PyInstaller。最终打包时额外加入：

- `tkinter` 与 `_tkinter`。
- Tcl/Tk 脚本目录。
- `tcl86t.dll` 与 `tk86t.dll`。
- rawpy 及其 LibRaw 依赖。

目前做过的自动检查包括：

1. Python 源码编译检查。
2. RAW+JPG 分组测试。
3. 仅 RAW、仅 JPG、RAW+JPG 导出成员选择测试。
4. 保留状态与模式独立性的测试。
5. 打包后的 `.exe --self-test` 无窗口运行库自检，退出码为 0。

按照使用者要求，没有通过 Computer Use 自动操作用户桌面做视觉测试；因此实际照片文件夹中的显示效果仍应由用户在自己的显示器上进行最后确认。

## 14. 当前边界

- 当前 RAW 专门支持 DNG；其他相机 RAW 格式尚未加入。
- 当前只扫描所选文件夹的第一层，不自动递归子文件夹。
- 软件是选片工具，不包含调色、裁剪、删除和照片修改功能。
- RAW+JPG 组的预览和放大使用 JPG，不提供手动切换查看 RAW 内嵌预览的功能。
- JPG 全量预载会使用较多内存，这是用内存换取浏览速度的设计选择。
